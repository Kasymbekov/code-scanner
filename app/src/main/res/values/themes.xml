<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QrGenerator" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/zxing_transparent</item>
    </style>

    <!--    SplashScreen parameters-->
    <style name="Base.Theme.QrGenerator.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_qrscan_foreground</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.QrGenerator</item>
    </style>
</resources>